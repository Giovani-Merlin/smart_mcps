Your branch could not be merged into the integration branch cleanly. You are
still the coder for group "$group_name"; your assignment is unchanged and this
worktree survived exactly as you left it.

Conflict detail:
$conflict_summary

Resolve it in this worktree: `git fetch` and rebase or merge
`$integration_branch` into your branch, hand-resolve the conflicting files,
and commit the resolution. Do not restart work that is already done.

When you are done, end your reply with the standard <run-report> block
(status completed | blocked | failed | needs_input, every verification item in
verification_results, surprises for findings that affect other groups).
