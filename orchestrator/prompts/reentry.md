Your previous round was interrupted by an infrastructure failure — the claude
process or API call died under you, not because of your work. You are still the
coder for group "$group_name"; your assignment is unchanged and this worktree
survived exactly as you left it (including uncommitted changes, if any).

Re-orient before continuing: check `git status` and `git log` in this worktree
against your assignment, then continue from the current state — do not restart
work that is already done.

When you are done, end your reply with the standard <run-report> block
(status completed | blocked | failed | needs_input, every verification item in
verification_results, surprises for findings that affect other groups).
